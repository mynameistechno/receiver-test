name: Comment on Stale Issues
on:
    schedule:
      - cron: '30 1 * * *'
  
jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/stale@v9
        with:
            days-before-close: -1
            days-before-issue-stale: 2
            